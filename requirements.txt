numpy
sounddevice
# AppKit, for the menu bar's status item. It used to arrive by accident, pulled in by
# the hotkey library this replaced; menubar.py imports AppKit directly, so it is
# declared directly.
pyobjc-framework-Cocoa
transformers
huggingface_hub
pywhispercpp
rich
torch
torchvision
torchaudio
pyannote.audio  # optional: speaker labels (needs a Hugging Face token; see README)
